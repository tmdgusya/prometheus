# forge — 인수조건 템플릿

align의 handoff 문서를 받아, forge 세션 끝에 이 템플릿을 채운다
(`skills/forge/SKILL.md` §1 참고). 완성된 목록을 사용자에게 보여주고
최종 확인을 받은 뒤 forge를 종료한다.

`---` 아래를 복사해 답글에 붙이고 괄호 필드를 채운다. 빈 칸을 두지
않는다 — 해당하지 않으면 "n/a"라고 쓰고 이유를 붙인다 (빈 칸은 "확인
안 함"으로 읽힌다).

---

# 인수조건: [작업 짧은 이름]

## 출처 handoff
[어느 align 세션의 handoff인지 — 한 줄. 파일명이나 작업명.]

## 완료 인수조건 (객관적, 검증 가능)
각 조건은 "실행 명령 → 예상 결과" 형태여야 한다. 주관어("깔끔하게",
"잘 동작")는 금지. 한 조건이 둘 이상의 검증으로 쪼개지면 쪼갠다.

- `실행 명령` → `예상 결과`
  - 예: `npm test auth/login` → 9 passed, exit 0
- `실행 명령` → `예상 결과`
- ...

## 범위 일치 검증
[forge §1-3 단계. scope ↔ 인수조건 양방향 교차검증 결과.]

- In scope에 있고 인수조건에도 있는 항목: [나열]
- In scope인데 인수조건에 빠진 항목 (있다면): [없어야 이상적]
- Out of scope인데 검증하려고 섞인 조건 (있다면): [제거 대상]

## 이 조건이 /goal 문장으로 옮겨갈 때
[각 인수조건을 prometheus §2-C 형식의 `/goal` 문장으로 옮겼을 때
그대로 쓸 수 있는지 확인. 모호한 부분이 남아 있으면 여기서 드러난다.]

## 확인됨
[체크박스로 둔다. 사용자가 전체를 검토하고 동의/수정한 뒤에만 표시.]

- [ ] 사용자가 이 인수조건 목록을 확인함
